apiVersion: external-secrets.io/v1beta1
kind: ClusterExternalSecret
metadata:
  name: granite-lightspeed-maas-secret
  namespace: external-secrets
spec:
  externalSecretName: "granite-lightspeed-maas-secret-generated"
  refreshTime: "1m"
  namespaceSelector:
    matchLabels:
      granite-lightspeed-maas: yes-please
  externalSecretSpec:
    secretStoreRef:
      kind: ClusterSecretStore
      name: akeyless-cluster-secret-store

    refreshInterval: 1h
    target:
      name: granite-lightspeed-maas
      creationPolicy: Owner
    dataFrom:
      - extract:
          key: /openshift-hub/granite-lightspeed-maas
