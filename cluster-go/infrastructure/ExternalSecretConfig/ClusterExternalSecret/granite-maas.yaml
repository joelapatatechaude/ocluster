apiVersion: external-secrets.io/v1beta1
kind: ClusterExternalSecret
metadata:
  name: granite-mass-secret
  namespace: external-secrets
spec:
  externalSecretName: "granite-mass-secret-generated"
  refreshTime: "1m"
  namespaceSelector:
    matchLabels:
      granite-mass: yes-please
  externalSecretSpec:
    secretStoreRef:
      kind: ClusterSecretStore
      name: akeyless-cluster-secret-store

    refreshInterval: 1h
    target:
      name: granite-mass
      creationPolicy: Owner
    dataFrom:
      - extract:
          key: /openshift-hub/granite-maas
