apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-cpu
  namespace: vllm-cpu-continue
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem
