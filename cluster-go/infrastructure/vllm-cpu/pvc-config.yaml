apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-cpu-config
  namespace: vllm-cpu
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
