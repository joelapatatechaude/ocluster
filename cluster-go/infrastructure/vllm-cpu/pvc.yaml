apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-cpu
  namespace: vllm-cpu
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem
