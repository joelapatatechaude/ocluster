apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tailscale-network.fullname" . }}-test-connection"
  labels:
    {{- include "tailscale-network.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tailscale-network.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
