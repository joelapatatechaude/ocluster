apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: patch-operator-serviceaccounts-patcher
rules:
- apiGroups: [""]
  resources: ["serviceaccounts"]
  verbs: ["get", "patch", "list"]
